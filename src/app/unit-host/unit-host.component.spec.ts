import { UnitHostComponent } from './unit-host.component';

describe('UnitHostComponent', () => {
  it('should create', () => {
    const component: UnitHostComponent = new UnitHostComponent(null, null);
    expect(component).toBeTruthy();
  });
});
